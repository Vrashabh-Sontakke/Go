// every go code lives inside a package
// code starts running from the main package
// every file inside the same folder must have the same package name
package main

// "fmt" is a package for formatted input/output
import "fmt"

// the main function serves as the starting point for the code execution
func main() {
	fmt.Println("Hello Gophers ☺")
}
